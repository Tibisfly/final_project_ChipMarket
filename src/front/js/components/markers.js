import React from "react";
import { Marker } from "react-leaflet";

const Markers = () => {
	return (
		<div>
			<Marker position={{ lat: "40.430598", lng: "-3.682766" }} />
		</div>
	);
};

export default Markers;
